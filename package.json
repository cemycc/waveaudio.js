{
  "name": "waveaudiojs",
  "version": "0.0.1",
  "description": "Wave Audio JS",
  "main": "src/waveaudio.ts",
  "scripts": {
    "build": "rollup -c",
    "lint": "tslint 'src/**/*.ts' -t verbose --force",
    "serve": "live-server --port=3000 --entry-file='examples/index.html' --watch='dist' --quiet --no-browser",
    "watch": "rollup -c --watch",
    "dev": "npm-run-all lint --parallel watch serve"
  },
  "author": "Emanuel Costea",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.0.0",
    "npm-run-all": "^2.3.0",
    "rollup": "^0.34.3",
    "rollup-plugin-typescript": "^0.7.7",
    "rollup-watch": "^2.5.0",
    "tslint": "^3.14.0",
    "typescript": "^1.8.10"
  },
  "dependencies": {
    "md5": "^2.1.0"
  }
}
