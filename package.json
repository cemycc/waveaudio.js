{
  "name": "waveaudiojs",
  "version": "0.0.1",
  "description": "Wave Audio JS",
  "main": "dist/waveaudio.js",
  "typings": "./dist/main",
  "scripts": {
    "build": "webpack",
    "lint": "tslint 'src/**/*.ts' -t verbose --force",
    "serve": "live-server --port=3000 --entry-file='examples/index.html' --watch='dist','examples' --quiet --no-browser",
    "watch": "webpack --watch",
    "dev": "npm-run-all lint --parallel watch serve"
  },
  "author": "Emanuel Costea",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^7.0.1",
    "@types/webaudioapi": "0.0.27",
    "live-server": "^1.0.0",
    "npm-run-all": "^2.3.0",
    "ts-loader": "^2.0.0",
    "tslint": "^3.14.0",
    "typescript": "^2.1.5",
    "webpack": "^2.2.0"
  },
  "dependencies": {
    "randomcolor": "^0.4.4",
    "tunajs": "^1.0.0"
  }
}
